export type OtherStoreType = {
    proxyListUpdate: number,
    otherUpdate: number,
    numUpdate: number,
    loadingMsg: string
    storeTime: number
    proxyStoreTime: number
    backupFileTime: number
}